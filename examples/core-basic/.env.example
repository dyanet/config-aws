# Copy to `.env` and adjust. Loaded by EnvFileLoader in index.mjs.
APP_NAME=config-aws-demo
PORT=3000
FEATURE_FLAG=true

# When using AWS sources, set the region (or rely on your AWS profile/instance role).
# AWS_REGION=us-east-1
# SECRET_NAME=/my-app/config
# SSM_PREFIX=/my-app
